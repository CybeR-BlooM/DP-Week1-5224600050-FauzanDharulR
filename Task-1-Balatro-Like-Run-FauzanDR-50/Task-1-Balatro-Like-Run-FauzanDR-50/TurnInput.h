#pragma once

struct TurnInput {
    int cardValue; // Anggap saja ini nilai kartu yang dimainkan (misal: 10)

    // Nanti bisa ditambah seperti: string cardSuit, bool isPlayed, dll.
    // Tapi untuk tugas arsitektur, satu variabel integer sudah cukup.
};